# Decisions Log — <PROJECT>

One row per locked decision. Ground truth for what is *not* up for re-debate. Append-only — supersede, never delete. Full reasoning in `decision-qa-log.md`.

| Date | Area | Decision | Revisit trigger |
|---|---|---|---|
