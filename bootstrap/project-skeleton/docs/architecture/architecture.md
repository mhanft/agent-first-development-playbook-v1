# <PROJECT> — Architecture

The canonical architecture description. The orchestrator reads this (plus `../../orchestration/architecture-drift-log.md`) instead of describing architecture from memory. Bumped at SDLC Step 6 on any structural change (new table, event/command type, module, command, state-machine or IPC change).

Diagrams live alongside this file in `architecture/`.

_Stub — populate at the first architecture lock._
