# Project Skeleton

These files are stamped into a new project at bootstrap (Step 3 of `../bootstrap-procedure.md`).

- `docs/*` → the project's `docs/`
- `orchestration/*` → the project's `orchestration/`

They start as stubs and grow as the project runs. Replace every `<PROJECT>` / `<…>` placeholder when stamping.
